[database]
engine = postgres
host = localhost
user = postgres
schema = postgres
port = 5432
password = secret321
url = ${database:engine}://${database:user}:${database:password}@${database:host}:${database:port}/${database:schema}